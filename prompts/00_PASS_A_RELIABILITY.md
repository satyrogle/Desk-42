# Codex Task 00 — Pass A Reliability and Correctness

Read `AGENTS.md`, `CODEX_START_HERE.md`, `docs/PROJECT_CONTEXT.md`, `docs/IMPLEMENTATION_PLAN.md`, and `docs/ACCEPTANCE_TESTS.md` before editing.

Implement **Pass A only**. Do not begin the Bureau Operations Console or authored-art work.

Required scope:

1. Fix `machine.html` box sizing, WebGL failure fallback, beat reveal independence, reduced-motion rendering, and visibility-based loop pause/resume.
2. Fix invisible rating fills and audit sibling fill elements.
3. Correct heading semantics across all three pages.
4. Replace stacked achievement toasts with a one-at-a-time FIFO queue.
5. Correct README, manifest, validation documentation, and add/restore the control-room generator so documentation matches the repository.
6. Run every Pass A check in `docs/ACCEPTANCE_TESTS.md`.

Before editing, inspect and report the exact current implementation points. Preserve visual design and factual content.

Do not claim a push, PR, screenshot, or test unless it actually occurred.

Deliver using `docs/COMPLETION_REPORT_TEMPLATE.md`, including screenshots for dark/light, mobile, rating fills, and forced WebGL fallback.
